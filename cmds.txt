# Commands executed for registry tool scenario

# Seed 20 tools (5 with similar descriptions) while port-forwarding the registry service
PORT_FORWARD=true \
NAMESPACE=mcp \
SERVICE_NAME=mcp-registry \
REMOTE_PORT=80 \
TOOL_OWNER=team-search \
TOOL_PREFIX=cli-demo \
./scripts/seed-tools.sh

# Fetch tools for the owner to verify seeding
PORT_FORWARD=true \
NAMESPACE=mcp \
SERVICE_NAME=mcp-registry \
REMOTE_PORT=80 \
OWNER=team-search \
./scripts/get-tools.sh

# Delete all tools for the owner
PORT_FORWARD=true \
NAMESPACE=mcp \
SERVICE_NAME=mcp-registry \
REMOTE_PORT=80 \
OWNER=team-search \
./scripts/delete-tools.sh

# Confirm cleanup
PORT_FORWARD=true \
NAMESPACE=mcp \
SERVICE_NAME=mcp-registry \
REMOTE_PORT=80 \
OWNER=team-search \
./scripts/get-tools.sh
